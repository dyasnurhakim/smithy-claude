---
description: "Unit-test the changed code per the stack playbook (alias of smithy:ring-test)"
argument-hint: "[same arguments as /smithy:ring-test]"
---

This command is a technical alias. Invoke the `smithy:ring-test` skill with the
Skill tool NOW and follow it exactly. Arguments to pass through: $ARGUMENTS
