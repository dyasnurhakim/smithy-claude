---
description: "Research & requirements analysis — explores the codebase, converts assumptions into questions, writes a spec (alias of smithy:assay)"
argument-hint: "[same arguments as /smithy:assay]"
---

This command is a technical alias. Invoke the `smithy:assay` skill with the
Skill tool NOW and follow it exactly. Arguments to pass through: $ARGUMENTS
