---
description: "One-shot fix lane: plan → confirm → forge (no TDD) → targeted tests → one report (alias of smithy:strike)"
argument-hint: "[items to fix]"
---

This command is a technical alias. Invoke the `smithy:strike` skill with the
Skill tool NOW and follow it exactly. Arguments to pass through: $ARGUMENTS
