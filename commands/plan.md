---
description: "Turn a spec into a verify-annotated implementation plan with per-task briefs and parallel batch markers (alias of smithy:blueprint)"
argument-hint: "[same arguments as /smithy:blueprint]"
---

This command is a technical alias. Invoke the `smithy:blueprint` skill with the
Skill tool NOW and follow it exactly. Arguments to pass through: $ARGUMENTS
