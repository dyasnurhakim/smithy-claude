---
description: "Two-verdict code review (spec compliance + code quality) with severity, confidence, and evidence per finding (alias of smithy:inspect)"
argument-hint: "[same arguments as /smithy:inspect]"
---

This command is a technical alias. Invoke the `smithy:inspect` skill with the
Skill tool NOW and follow it exactly. Arguments to pass through: $ARGUMENTS
