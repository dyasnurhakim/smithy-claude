---
description: "Run the full smithy dev pipeline (research → plan → implement → review → test) with approval gates (alias of smithy:smithy)"
argument-hint: "[same arguments as /smithy:smithy]"
---

This command is a technical alias. Invoke the `smithy:smithy` skill with the
Skill tool NOW and follow it exactly. Arguments to pass through: $ARGUMENTS
