---
description: "Stress/load-test a running service against user-set thresholds (alias of smithy:proof)"
argument-hint: "[same arguments as /smithy:proof]"
---

This command is a technical alias. Invoke the `smithy:proof` skill with the
Skill tool NOW and follow it exactly. Arguments to pass through: $ARGUMENTS
