---
description: "Profile and benchmark hot paths against a baseline — recommendations only (alias of smithy:hone)"
argument-hint: "[same arguments as /smithy:hone]"
---

This command is a technical alias. Invoke the `smithy:hone` skill with the
Skill tool NOW and follow it exactly. Arguments to pass through: $ARGUMENTS
